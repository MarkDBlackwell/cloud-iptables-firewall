# Handle authorized remote IP's.

# Add whatever other ports you want.

# Open SSH port 22
for r in $ipv4_authorized_remote_ips
do
  $ipt4 -A INPUT  -p tcp -i $ext_interf      --source $r --dport 22 -j ACCEPT
  $ipt4 -A OUTPUT -p tcp -o $ext_interf --destination $r --sport 22 -j ACCEPT
done
